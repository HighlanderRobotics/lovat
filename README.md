# Lovat

Lovat is Highlander Robotics Team 8033's scouting system. This repository is being assembled from the Server, Dashboard, Collection, and Website repositories.

The migration preserves each application's default-branch history and keeps the applications independently buildable under `apps/`. See `docs/migration/` for the frozen source revisions, verification evidence, and deployment cutover record.
